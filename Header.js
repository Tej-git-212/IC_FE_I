import React from 'react'

const Header = () => {
    return (
        <div>
            <header className="App-header">
                  Use React to display photos using JSON placeholder API in an infinite scroll View
            </header>
        </div>
    )
}

export default Header
